<?php
$file = './codetest.txt';
if($_POST){
		ob_start();
		var_dump($_POST);

		$str=ob_get_clean();
		$current .= date('Y-m-d H:i:s',time());
		$current .= $str;
		$oldStr = file_get_contents($file);
		$current .= $oldStr;
		// Write the contents back to the file
		file_put_contents($file, $current);
		//$temdata = file_get_contents($file);
		//var_dump($temdata);
}
else{
	if($_GET['ac']=='1'){
		file_put_contents($file, '');
	}
	$temdata = file_get_contents($file);
	var_dump($temdata);
}
